// React integration is in a separate file to allow tree-shaking
// Users import from 'manimweb/react' or 'manimweb/integrations/react'

// Note: Do not export react.tsx here to keep the main bundle free of React
// React users should import directly from 'manimweb/react'
